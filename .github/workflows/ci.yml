# Purpose: Disable legacy docker-ci so it no longer auto-triggers on PRs.
# Safe & reversible: it can only be run manually now.
name: docker-ci (legacy disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy docker-ci disabled. Use .github/workflows/docker-ci.yml"
